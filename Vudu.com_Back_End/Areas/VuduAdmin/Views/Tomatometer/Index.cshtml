@model List<Tomatometer>

@{
    ViewData["Title"] = "Index";
}

<div class=" mt-4">
    <a class="btn btn-dark" asp-controller="Tomatometer" asp-action="Create">Create</a>
</div>
<table class="table">
    <thead>
        <tr>
            <th scope="col">Id</th>
            <th scope="col">Title</th>
        </tr>
    </thead>
    <tbody>
        @foreach (Tomatometer rt in Model)
        {
            <tr>
                <td>@rt.Id</td>
                <td>@rt.Title</td>
                <td>
                    <div class="d-flex justify-content-around">
                        <div>
                            <a class="mx-2" style="color:red; font-size:25px;" asp-controller="Tomatometer" asp-action="Detail" asp-route-Id="@rt.Id"><i class="fa-solid fa-circle-info"></i></a>
                        </div>

                        <div>
                            <a style="color:red; font-size:25px;" asp-controller="Tomatometer" asp-action="Edit" asp-route-Id="@rt.Id"><i class="fa-solid fa-pen-to-square"></i></a>
                        </div>

                        <div class="mx-2">
                            <a style="color:red; font-size:25px;" asp-controller="Tomatometer" asp-action="Delete" asp-route-Id="@rt.Id"><i class="fa-solid fa-trash-can"></i></a>
                        </div>
                    </div>

                </td>
            </tr>
        }

    </tbody>
</table>








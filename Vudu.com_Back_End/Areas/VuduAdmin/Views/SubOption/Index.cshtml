@model List<SubOption>
@{
    ViewData["Title"] = "Index";
}

<div class=" mt-4">
    <a class="btn btn-dark" asp-controller="SubOption" asp-action="Create">Create</a>
</div>
<table class="table">
    <thead>
        <tr>
            <th scope="col">Image</th>
            <th scope="col">ImageName</th>
            <th scope="col">Title</th>
            <th scope="col">SubTitle</th>
             <th scope="col">Main Option</th>
        </tr>
    </thead>
    <tbody>
        @foreach (SubOption sub in Model)
        {
            <tr>
                <td>
                    <img src="~/assets/img/Mov-Img-Navbar/@sub.Image"/>
                </td>
                <td>@sub.ImageName</td>
                <td>@sub.Title</td>
                 <td>@sub.SubTitle</td>
                 <td>@sub.MainOptionId</td>
                <td>
                    <div class="d-flex justify-content-around">
                        <div>
                            <a class="mx-2" style="color:red; font-size:25px;" asp-controller="SubOption" asp-action="Detail" asp-route-Id="@sub.Id"><i class="fa-solid fa-circle-info"></i></a>
                        </div>

                        <div>
                            <a style="color:red; font-size:25px;" asp-controller="SubOption" asp-action="Edit" asp-route-Id="@sub.Id"><i class="fa-solid fa-pen-to-square"></i></a>
                        </div>

                        <div class="mx-2">
                            <a style="color:red; font-size:25px;" asp-controller="SubOption" asp-action="Delete" asp-route-Id="@sub.Id"><i class="fa-solid fa-trash-can"></i></a>
                        </div>
                    </div>

                </td>
            </tr>
        }

    </tbody>
</table>

